// TempleOS-Web package entry. Upstream files retain their embedded sprites.
#exe {Cd(__DIR__);}
Bool browser_auto_complete=AutoComplete(OFF);
#include "Coach.HC";
AutoComplete(browser_auto_complete);
